You are acting as a repository inspection tool.

Your job is NOT to analyze, refactor, modify, or improve the project.

Your ONLY task is to inspect the STM32 bootloader project and generate a complete, structured snapshot of the repository so another AI (ChatGPT) can understand the current state of development.

========================
PHASE 1 - Repository Tree
========================

Print the complete directory tree.

Include:
- folders
- source files
- header files
- linker scripts
- startup files
- CubeMX files
- configuration files

Do not omit anything.

========================
PHASE 2 - File Summary
========================

For EVERY source/header file, provide:

- Path
- Purpose
- Approximate number of lines
- Main functions
- Global variables
- Structures
- Enums
- Macros
- Dependencies (#include)

========================
PHASE 3 - Source Dump
========================

Now dump the project source.

For EVERY project file:

-----------------------------------
FILE:
<relative path>
-----------------------------------

```c
<complete contents>
```

Do this for:

- .c
- .h
- .ld
- .ioc
- startup assembly
- configuration files

If a file is very large (>500 lines), split it into multiple sequential parts:

FILE: Core/Src/main.c
Part 1/3

...

FILE: Core/Src/main.c
Part 2/3

...

Continue until every file has been completely dumped.

Do NOT summarize.
Do NOT omit code.
Do NOT truncate.

========================
PHASE 4 - Memory Layout
========================

Extract and print:

- Flash regions
- RAM regions
- Bootloader start
- Bootloader end
- Application start
- Stack
- Heap
- Vector table location

========================
PHASE 5 - UART
========================

List every UART-related function.

Include:

- Initialization
- IRQ handlers
- Transmit
- Receive
- Call locations

========================
PHASE 6 - Flash Programming
========================

List every function related to:

- Flash unlock
- Flash erase
- Flash write
- Flash lock
- Address calculations

========================
PHASE 7 - Boot Process
========================

Trace the boot process from reset until application jump.

Show the function call order.

Example:

Reset_Handler
↓

SystemInit
↓

main()
↓

...

↓

JumpToApplication()

========================
OUTPUT FORMAT
========================

Produce everything in Markdown.

Do NOT analyze.

Do NOT recommend changes.

Do NOT explain.

Do NOT rewrite code.

Only expose the current project exactly as it exists.